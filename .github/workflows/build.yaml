name: Build URCap (jar + urcap)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v3

      - name: Debug Show pom.xml path
        run: |
          echo "📄 pom.xml 위치 확인:"
          ls -l ${{ github.workspace }}/com.ur.thph.modbus_urcap/pom.xml

      - name: Build .jar using URCap SDK Docker
        run: |
          docker run --rm \
            -v ${{ github.workspace }}/com.ur.thph.modbus_urcap:/workspace \
            -w /workspace \
            mohamedghita/build:urcap_java_build \
            mvn clean install

      - name: Create .urcap package (no script)
        run: |
          cd com.ur.thph.modbus_urcap
          mkdir -p target/urcap_tmp

          echo "📦 파일 복사"
          cp target/Modbus_URCap-1.0.0.jar target/urcap_tmp/ || true
          cp -r daemon target/urcap_tmp/ 2>/dev/null || true
          cp src/main/resources/urcap.xml target/urcap_tmp/ 2>/dev/null || true

          echo "📦 zip 압축"
          cd target/urcap_tmp
          zip -r ../Modbus_URCap-1.0.0.urcap * > /dev/null
          cd ../..
          echo "✅ .urcap 파일 생성 완료"

      - name: Upload .jar Artifact
        uses: actions/upload-artifact@v4
        with:
          name: urcap-jar
          path: com.ur.thph.modbus_urcap/target/*.jar

      - name: Upload .urcap Artifact
        uses: actions/upload-artifact@v4
        with:
          name: urcap-zip
          path: com.ur.thph.modbus_urcap/target/*.urcap
